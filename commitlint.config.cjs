// Keep commit history machine-readable with the Conventional Commits format.
// Examples: "feat: add expiry cleanup" or "fix(server): reject bad zip paths".
module.exports = {
  extends: ["@commitlint/config-conventional"],
};
